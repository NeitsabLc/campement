services:
  php:
    image: "${CAMP_RELEASE_PHP_IMAGE:?CAMP_RELEASE_PHP_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing

  nginx:
    image: "${CAMP_RELEASE_NGINX_IMAGE:?CAMP_RELEASE_NGINX_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing

  maintenance:
    image: "${CAMP_RELEASE_PHP_IMAGE:?CAMP_RELEASE_PHP_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing

  backup:
    image: "${CAMP_RELEASE_BACKUP_IMAGE:?CAMP_RELEASE_BACKUP_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing

  database:
    image: "${CAMP_RELEASE_POSTGRES_IMAGE:?CAMP_RELEASE_POSTGRES_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing

  liquibase:
    image: "${CAMP_RELEASE_LIQUIBASE_IMAGE:?CAMP_RELEASE_LIQUIBASE_IMAGE doit contenir une reference GHCR par digest}"
    build: !reset null
    pull_policy: missing
