import { Controller } from '@hotwired/stimulus';
export default class extends Controller {
 static targets=['template','recipeTemplate','catalog','recipes'];
 connect(){this.element.style.setProperty('--public-count',this.element.dataset.publicCount);this.foods=JSON.parse(this.catalogTarget.textContent);this.recipeData=JSON.parse(this.recipesTarget.textContent);this.refresh();}
 addFood(event){const block=event.currentTarget.closest('[data-menu-block]');const select=block.querySelector('[data-food-picker]');if(select.value)this.addLine({denree:select.value,quantites:{}},block.querySelector('[data-extra-rows]'),block.dataset.category||'');select.value='';select.dispatchEvent(new Event('change',{bubbles:true}));}
 addRecipe(event){const block=event.currentTarget.closest('[data-menu-block]');const select=block.querySelector('[data-recipe-picker]');const id=select.value;const r=this.recipeData[id];if(r){const group=this.makeRecipeGroup(id,r.nom);r.lignes.forEach(l=>this.addLine({...l,recette:id,recette_instance:group.dataset.recipeInstance},group.querySelector('[data-recipe-rows]'),block.dataset.category||''));block.querySelector('[data-recipe-groups]').append(group);this.refresh();}select.value='';}
 makeRecipeGroup(id,name){const group=this.recipeTemplateTarget.content.firstElementChild.cloneNode(true);group.dataset.recipe=id;group.dataset.recipeInstance=crypto.randomUUID();group.querySelector('[data-recipe-name]').textContent=name;return group;}
 addLine(data,container,category){const box=document.createElement('div');box.innerHTML=this.templateTarget.innerHTML.trim();const row=box.firstElementChild;row.querySelector('[data-field="denree"]').value=data.denree;row.querySelector('[data-field="categorie"]').value=category;row.querySelector('[data-field="recette"]').value=data.recette||'';row.querySelector('[data-field="recette_instance"]').value=data.recette_instance||'';row.dataset.conditionnement=data.conditionnement||'';Object.entries(data.quantites||{}).forEach(([p,v])=>{const x=row.querySelector(`[data-public="${p}"]`);if(x)x.value=String(v).replace('.',',');});container.append(row);this.refresh();}
 remove(e){e.currentTarget.closest('[data-line]').remove();this.refresh();}
 removeRecipe(e){e.currentTarget.closest('[data-recipe-group]').remove();this.refresh();}
 resetRecipe(e){const group=e.currentTarget.closest('[data-recipe-group]');const r=this.recipeData[group.dataset.recipe];if(!r)return;const rows=group.querySelector('[data-recipe-rows]');rows.replaceChildren();r.lignes.forEach(l=>this.addLine({...l,recette:group.dataset.recipe,recette_instance:group.dataset.recipeInstance},rows,group.closest('[data-menu-block]').dataset.category||''));this.refresh();}
 refresh(){[...this.element.querySelectorAll('[data-line]')].forEach((row,i)=>{const id=row.querySelector('[data-field="denree"]').value;const d=this.foods[id];const name=row.querySelector('[data-name]');if(name)name.textContent=d?.nom||'';const s=row.querySelector('[data-field="conditionnement"]');const selected=s.dataset.selected||row.dataset.conditionnement||s.value;s.innerHTML=(d?.conditionnements||[]).map(u=>`<option value="${u.id}" ${u.id===selected?'selected':''}>${u.nom}</option>`).join('');delete s.dataset.selected;['denree','conditionnement','categorie','recette','recette_instance'].forEach(field=>{const x=row.querySelector(`[data-field="${field}"]`);x.name=`lignes[${i}][${field}]`;});row.querySelectorAll('[data-public]').forEach(x=>x.name=`lignes[${i}][quantites][${x.dataset.public}]`);});}
}
