# Connexions d'administration depuis le conteneur PostgreSQL uniquement.
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Replication locale uniquement.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust

# Application Symfony et Liquibase via le reseau Docker de production.
host    campement       campement       172.18.0.0/16           scram-sha-256

# Utilisateur nominatif depuis le LAN.
host    campement       blecaer         192.168.2.0/24          scram-sha-256

# Utilisateur nominatif depuis son profil WireGuard Freebox.
host    campement       blecaer         192.168.27.65/32        scram-sha-256

# Toute autre connexion TCP est refusee explicitement.
host    all             all             0.0.0.0/0               reject
host    all             all             ::/0                    reject
